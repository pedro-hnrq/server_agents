services:
  nlw-agents-pg:
    image: pgvector/pgvector:pg17
    container_name: agents-pg
    environment:
      POSTGRES_USER: dev
      POSTGRES_PASSWORD: Dev$321
      POSTGRES_DB: agents
    ports:
      - "5433:5432"
    volumes:
      - ./docker/setup.sql:/docker-entrypoint-initdb.d/setup.sql